<h1><%= current_user.is_manager? ? "Editing Todo" : "Update todo status"%></h1>

<%= render 'form', todo: @todo %>

<%if current_user.is_manager?%>
	<%= link_to 'Show', @todo %> |
	<%= link_to 'Back', todos_path %>
<%end%>
